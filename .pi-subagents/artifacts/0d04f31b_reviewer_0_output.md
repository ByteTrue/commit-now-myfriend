## Review

- **NON-BLOCKER — Correct: unseen shadow gate 已解除。** GitHub issue #34 当前 body 的 “Independent evaluation isolation” 明确要求独立 reviewer 在选训练数据前冻结 post-2024-11-18、完整、多文件、canonical repo 隔离的 unread gate；训练方只拿 hash/rubric/coverage/exclusions/count，不拿 raw diff、gold 或 Guidance wording；shadow 反馈一旦改变训练、prompt、renderer 等即作废并更换新 gate（issue body L92–105）。Epic 也明确 GO 依赖训练方不可见、一次性独立 shadow/historical gate，且当前尚无通过门槛的模型（`.cs/epics/001-o-offline-commit-flow/spec.md:164,170,188-190`）。
- **NON-BLOCKER — Correct: renderer 输入边界已解除。** API 被收窄为 `render(resolvedStyle, semanticRecord)`，禁止读取 diff、Guidance、history、expected output、case ID、gold metadata，也禁止修补 subject/body；type 缺失/无效直接拒绝，scope 不得推断，raw record 与 rendered message 分别计分并有 mutation self-tests（issue body L40–62）。Epic 的产品边界一致：只允许机械添加前缀/分隔和连接 body，不能改写、补造或掩盖语义/Guidance 失败（`.cs/epics/001-o-offline-commit-flow/spec.md:55-67`）。
- **NON-BLOCKER — Correct: 数据验收 blocker 已解除。** issue 固定唯一数据源及 revision/license allowlist，要求完整 `mods[]`、整 commit 丢弃策略、repo/fork 隔离、跨 train/validation/test/shadow/historical 的多重泄漏检测、全量 grounding/secret/PII/duplicate 检查，并以固定 seed 盲审 200 条；门槛为 0 critical 且至少 95% fully grounded，否则重新设计并以新冻结 seed 重采样（issue body L64–90）。Epic 将“完整多文件数据可信”保留为尚待执行的 gate，而非宣称已有结果（`.cs/epics/001-o-offline-commit-flow/spec.md:180,186-190`）。
- **NON-BLOCKER — Correct: M5 硬件门已解除。** issue 要求每个预声明配置跑等同真实训练路径的 smoke，并同时记录 RSS、MLX 分配、memory pressure、pageout/compression/swap、step time 和预计总时长；继续 Mac 训练的定量门为 MLX peak < 80%、无归因于本次运行的 pageout/swapout、median step ≤ 2s、单配置 ≤ 6h，失败则训练前 STOP 并做固定环境的 RTX 5080/WSL2 handoff（issue body L123–153）。Epic 对这些阈值和“硬件失败不等于质量失败”的表述一致（`.cs/epics/001-o-offline-commit-flow/spec.md:164,188`）。
- **NON-BLOCKER — Correct: 配置冻结与 clean retrain 已解除。** 两个训练配置和 checkpoint selection rule 必须在任何 base/regression/shadow 输出前冻结；只能用 train/validation 选 BF16 checkpoint；选定配置从空 run directory clean retrain 后才可进入一次性 shadow；重现容差也要求预声明（issue body L96–105）。风险顺序再次要求所有 freeze 先于模型输出，且 clean retrain 后不得查看 shadow 数据（issue body L165–187）。
- **NON-BLOCKER — Correct: 700 MB skeleton 已解除。** issue 将 skeleton 放在训练前：用固定 Q5 reference、stripped runtime 及传递非系统库、launcher/parser/renderer stub、license/notices 组成目标 macOS 安装布局，按 logical installed bytes 计入重复 wrapper/package 文件；超过 700,000,000 bytes 或没有 documented growth margin 即停止，并在质量通过后用实际 fused Q5 重测（issue body L169–175, L214–222）。Epic 同样把上限定义为完整可安装发行物并要求分项报告模型、runtime、launcher、条款/notice（`.cs/epics/001-o-offline-commit-flow/spec.md:123-125`）。
- **NON-BLOCKER — Note: 当前没有模型输出、shadow/data 正在独立构建，不构成本轮规格 blocker。** 本复审确认的是六项门禁定义及顺序已足以阻止泄漏、越界渲染、未验收数据、未过硬件、配置漂移和超体积训练；实际 gate evidence 仍必须按 issue 产出后才能 GO。Epic 当前状态准确写为“当前没有通过门槛的 Bundled Local Model”，并禁止在 #34 全部门通过前建设 Git 写入流程（`.cs/epics/001-o-offline-commit-flow/spec.md:168-172`）。
- **PASS — 上一轮六个 blocker 均已解除；当前 body 与 Epic 无会重新打开这些 blocker 的冲突。**