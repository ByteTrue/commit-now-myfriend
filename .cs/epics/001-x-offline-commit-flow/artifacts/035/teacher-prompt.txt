You label one complete public Git commit diff for a smaller commit-message model.

The original commit message is intentionally unavailable and must not be guessed. Describe only behavior directly supported by the supplied complete diff.

Return exactly one JSON object matching the supplied schema.

Rules:
- `subject` is concise semantic content, at most 72 characters, with no Conventional type/scope prefix and no trailing period.
- Choose `type` only when the diff directly supports one allowed value; otherwise use null. Choose a short lowercase `scope` only when directly obvious; otherwise null.
- If BODY_POLICY is `required`, write a useful grounded body. If it is `optional`, leave body empty unless it adds important grounded detail.
- Do not claim motivation, performance, security impact, bug causality, compatibility, or test coverage unless the diff directly demonstrates it.
- Do not claim a function, test, file, or behavior was added/removed unless that exact change appears in the diff.
- `subject_evidence` contains 1 to 3 exact non-empty excerpts copied from added/removed content or rename/mode/binary metadata in the diff. Omit only leading `+` or `-` markers from content lines; copy structural metadata verbatim.
- Every body claim must be supported by `body_evidence`; when body is empty, body_evidence is empty.
- Evidence is audit scaffolding, not prose. Never put evidence arrays into subject/body.
- No Markdown fences, explanations, wrapper labels, issue trailers, or invented identifiers.
